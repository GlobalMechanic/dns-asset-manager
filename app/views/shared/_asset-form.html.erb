<% # text_field_tag :title, nil, :placeholder => 'Title' %>
<%= text_area_tag :description, nil, :placeholder => 'Description' %>

<%= select :asset_type, {}, Asset::TYPES %>
<%= select :scene_ids, {}, Scene.all.map {|x| [x.episode.number.pad + ' - ' + x.episode.title + ' - Scene ' + x.number.pad, x.id]}, {}, :multiple => true, :class => 'inline' %>

<%= autocomplete_field_tag :keyword_list, '', autocomplete_tag_name_assets_path, :"data-delimiter" => ', ', :multiple => true, :placeholder => 'Keyword List' %>
