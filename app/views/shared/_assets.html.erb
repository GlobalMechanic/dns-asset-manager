<div class="reel-id assets slots row<%= ' list-view' if @layout == 'list' %>">
<% assets.each_with_index do |asset, index| %>
  <% unless @layout == 'list' %>
  <div id="slot-<%= index %>" class="slot">
    <div class="region"></div>
  </div>
  <% end %>
  <%= render :partial => (@layout == 'list' ? 'shared/list' : 'shared/tile'), :locals => { :reel => @reel, :asset => asset } %>
<% end %>
  <% unless @layout == 'list' %>
  <div id="slot-<%= assets.length  %>" class="slot">
    <div class="region"></div>
  </div>
  <% end %>
</div>
